name: e734fe2bdae408bedbcc5b5b9ea9bbc0
fullyQualifiedName: hktest.e734fe2bdae408bedbcc5b5b9ea9bbc0
incrementalChangeDescription:
  fieldsAdded: []
  fieldsUpdated: []
  fieldsDeleted:
    - name: queryUsedIn
      oldValue: "[{\"id\":\"0b565859-50f2-4c96-b4f3-2abd5feeecb1\",\"type\":\"table\"\
        ,\"name\":\"LINEITEM\",\"fullyQualifiedName\":\"hktest.SNOWFLAKE_SAMPLE_DATA.TPCH_SF1.LINEITEM\"\
        ,\"description\":\"Lineitem data as defined by TPC-H\",\"displayName\":\"\
        LINEITEM\",\"deleted\":false}]"
  previousVersion: 0.1
  changeSummary: {}
owners: []
duration: 164.0
users: []
query: "SELECT count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"4.000 to 4.132\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"4.132 to 4.264\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"4.264 to 4.396\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"4.396 to 4.528\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"4.528 to 4.660\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"4.660 to 4.792\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"4.792 to 4.924\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"4.924 to 5.057\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"5.057 to 5.189\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"5.189 to 5.321\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"5.321 to 5.453\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"5.453 to 5.585\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"5.585 to 5.717\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"5.717 to 5.849\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"5.849 to 5.981\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"5.981 to 6.113\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"6.113 to 6.245\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"6.245 to 6.377\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"6.377 to 6.509\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"6.509 to 6.641\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"6.641 to 6.773\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"6.773 to 6.905\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"6.905 to 7.038\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"7.038 to 7.170\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"7.170 to 7.302\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"7.302 to 7.434\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"7.434 to 7.566\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"7.566 to 7.698\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"7.698 to 7.830\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"7.830 to 7.962\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"7.962 to 8.094\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"8.094 to 8.226\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"8.226 to 8.358\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"8.358 to 8.490\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"8.490 to 8.622\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"8.622 to 8.754\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"8.754 to 8.887\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"8.887 to 9.019\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"9.019 to 9.151\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"9.151 to 9.283\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"9.283 to 9.415\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"9.415 to 9.547\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"9.547 to 9.679\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"9.679 to 9.811\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"9.811 to 9.943\"\
  , count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?)\
  \ THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"9.943 to 10.075\", count(CASE WHEN (LEN(\"\
  L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"10.075 to 10.207\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"10.207 to\
  \ 10.339\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"10.339 to 10.471\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"10.471 to 10.603\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"10.603 to\
  \ 10.735\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"10.735 to 10.868\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"10.868 to 11.000\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"11.000 to\
  \ 11.132\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"11.132 to 11.264\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"11.264 to 11.396\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"11.396 to\
  \ 11.528\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"11.528 to 11.660\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"11.660 to 11.792\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"11.792 to\
  \ 11.924\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"11.924 to 12.056\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"12.056 to 12.188\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"12.188 to\
  \ 12.320\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"12.320 to 12.452\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"12.452 to 12.584\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"12.584 to\
  \ 12.716\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"12.716 to 12.849\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"12.849 to 12.981\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"12.981 to\
  \ 13.113\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"13.113 to 13.245\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"13.245 to 13.377\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"13.377 to\
  \ 13.509\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"13.509 to 13.641\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"13.641 to 13.773\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"13.773 to\
  \ 13.905\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"13.905 to 14.037\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"14.037 to 14.169\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"14.169 to\
  \ 14.301\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"14.301 to 14.433\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"14.433 to 14.565\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"14.565 to\
  \ 14.698\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"14.698 to 14.830\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"14.830 to 14.962\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"14.962 to\
  \ 15.094\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"15.094 to 15.226\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"15.226 to 15.358\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"15.358 to\
  \ 15.490\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"15.490 to 15.622\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"15.622 to 15.754\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"15.754 to\
  \ 15.886\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"15.886 to 16.018\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"16.018 to 16.150\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"16.150 to\
  \ 16.282\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"16.282 to 16.414\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"16.414 to 16.546\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND\
  \ LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"16.546 to\
  \ 16.679\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\"\
  ) < ?) THEN LEN(\"L_SHIPINSTRUCT\") END) AS \"16.679 to 16.811\", count(CASE WHEN\
  \ (LEN(\"L_SHIPINSTRUCT\") >= ? AND LEN(\"L_SHIPINSTRUCT\") < ?) THEN LEN(\"L_SHIPINSTRUCT\"\
  ) END) AS \"16.811 to 16.943\", count(CASE WHEN (LEN(\"L_SHIPINSTRUCT\") >= ?) THEN\
  \ LEN(\"L_SHIPINSTRUCT\") END) AS \"16.943 and up\" \nFROM \"TPCH_SF1\".\"LINEITEM\"\
  \n LIMIT ?"
checksum: e734fe2bdae408bedbcc5b5b9ea9bbc0
queryDate: 1747526400000
usedBy:
  - TESTOPENMETADATA
tags: []
queryUsedIn:
  - id: 0b565859-50f2-4c96-b4f3-2abd5feeecb1
    type: table
    name: LINEITEM
    fullyQualifiedName: hktest.SNOWFLAKE_SAMPLE_DATA.TPCH_SF1.LINEITEM
    description: Lineitem data as defined by TPC-H
    displayName: LINEITEM
    deleted: false
processedLineage: false
service:
  id: 28293590-2cd0-4d6a-ad7c-bf50802ee2dd
  type: databaseService
  name: hktest
  fullyQualifiedName: hktest
