name: 80893701afda7b40fd7e87ec363c3643
fullyQualifiedName: snowflake_test.80893701afda7b40fd7e87ec363c3643
incrementalChangeDescription:
  fieldsAdded: []
  fieldsUpdated:
    - name: usedBy
      oldValue: "[]"
      newValue: "[\"TESTOPENMETADATA\"]"
  fieldsDeleted: []
  previousVersion: 0.1
owners: []
duration: 22242.0
users: []
query: "WITH \"7492547a05b84d643fd227e52ada40ff_rnd\" AS \n(SELECT \"CATALOG_RETURNS_1\"\
  .\"CR_ORDER_NUMBER\" AS \"CR_ORDER_NUMBER\" \nFROM \"TPCDS_SF10TCL\".\"CATALOG_RETURNS\"\
  \ AS \"CATALOG_RETURNS_1\" TABLESAMPLE bernoulli(?)), \n\"7492547a05b84d643fd227e52ada40ff_sample\"\
  \ AS \n(SELECT \"7492547a05b84d643fd227e52ada40ff_rnd\".\"CR_ORDER_NUMBER\" AS \"\
  CR_ORDER_NUMBER\" \nFROM \"7492547a05b84d643fd227e52ada40ff_rnd\")\n SELECT count(CASE\
  \ WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\"\
  \ END) AS \"2.000 to 50.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"\
  CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"50.000M to 100.000M\"\
  , count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"\
  CR_ORDER_NUMBER\" END) AS \"100.000M to 150.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\"\
  \ >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"150.000M\
  \ to 200.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\"\
  \ < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"200.000M to 250.000M\", count(CASE WHEN\
  \ (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\"\
  \ END) AS \"250.000M to 300.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND\
  \ \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"300.000M to 350.000M\"\
  , count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"\
  CR_ORDER_NUMBER\" END) AS \"350.000M to 400.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\"\
  \ >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"400.000M\
  \ to 450.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\"\
  \ < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"450.000M to 500.000M\", count(CASE WHEN\
  \ (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\"\
  \ END) AS \"500.000M to 550.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND\
  \ \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"550.000M to 600.000M\"\
  , count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"\
  CR_ORDER_NUMBER\" END) AS \"600.000M to 650.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\"\
  \ >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"650.000M\
  \ to 700.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\"\
  \ < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"700.000M to 750.000M\", count(CASE WHEN\
  \ (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\"\
  \ END) AS \"750.000M to 800.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND\
  \ \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"800.000M to 850.000M\"\
  , count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"\
  CR_ORDER_NUMBER\" END) AS \"850.000M to 900.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\"\
  \ >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"900.000M\
  \ to 950.000M\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\"\
  \ < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"950.000M to 1000.000M\", count(CASE WHEN\
  \ (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\"\
  \ END) AS \"1000.000M to 1.050B\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND\
  \ \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"1.050B to 1.100B\"\
  , count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"\
  CR_ORDER_NUMBER\" END) AS \"1.100B to 1.150B\", count(CASE WHEN (\"CR_ORDER_NUMBER\"\
  \ >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"1.150B to\
  \ 1.200B\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" <\
  \ ?) THEN \"CR_ORDER_NUMBER\" END) AS \"1.200B to 1.250B\", count(CASE WHEN (\"\
  CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END)\
  \ AS \"1.250B to 1.300B\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\"\
  \ < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"1.300B to 1.350B\", count(CASE WHEN (\"\
  CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END)\
  \ AS \"1.350B to 1.400B\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\"\
  \ < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"1.400B to 1.450B\", count(CASE WHEN (\"\
  CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\" < ?) THEN \"CR_ORDER_NUMBER\" END)\
  \ AS \"1.450B to 1.500B\", count(CASE WHEN (\"CR_ORDER_NUMBER\" >= ? AND \"CR_ORDER_NUMBER\"\
  \ < ?) THEN \"CR_ORDER_NUMBER\" END) AS \"1.500B to 1.550B\", count(CASE WHEN (\"\
  CR_ORDER_NUMBER\" >= ?) THEN \"CR_ORDER_NUMBER\" END) AS \"1.550B and up\" \nFROM\
  \ \"7492547a05b84d643fd227e52ada40ff_sample\"\n LIMIT ?"
checksum: 80893701afda7b40fd7e87ec363c3643
queryDate: 1740614400000
usedBy:
  - TESTOPENMETADATA
tags: []
queryUsedIn:
  - id: e9119d1e-971c-4d36-8713-0798a54f4faa
    type: table
    name: CATALOG_RETURNS
    fullyQualifiedName: snowflake_test.SNOWFLAKE_SAMPLE_DATA.TPCDS_SF10TCL.CATALOG_RETURNS
    displayName: CATALOG_RETURNS
    deleted: false
processedLineage: false
service:
  id: 6d146814-0a2b-4296-ba8d-05f0c5888c40
  type: databaseService
  name: snowflake_test
  fullyQualifiedName: snowflake_test
