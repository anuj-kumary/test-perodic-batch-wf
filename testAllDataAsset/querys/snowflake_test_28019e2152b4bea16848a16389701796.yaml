name: 28019e2152b4bea16848a16389701796
fullyQualifiedName: snowflake_test.28019e2152b4bea16848a16389701796
incrementalChangeDescription:
  fieldsAdded: []
  fieldsUpdated:
    - name: usedBy
      oldValue: "[]"
      newValue: "[\"TESTOPENMETADATA\"]"
  fieldsDeleted: []
  previousVersion: 0.1
owners: []
duration: 78731.0
users: []
query: "WITH \"7492547a05b84d643fd227e52ada40ff_rnd\" AS \n(SELECT \"CATALOG_RETURNS_1\"\
  .\"CR_RETURNING_CUSTOMER_SK\" AS \"CR_RETURNING_CUSTOMER_SK\" \nFROM \"TPCDS_SF10TCL\"\
  .\"CATALOG_RETURNS\" AS \"CATALOG_RETURNS_1\" TABLESAMPLE bernoulli(?)), \n\"7492547a05b84d643fd227e52ada40ff_sample\"\
  \ AS \n(SELECT \"7492547a05b84d643fd227e52ada40ff_rnd\".\"CR_RETURNING_CUSTOMER_SK\"\
  \ AS \"CR_RETURNING_CUSTOMER_SK\" \nFROM \"7492547a05b84d643fd227e52ada40ff_rnd\"\
  )\n SELECT count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\"\
  \ < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"1.000 to 2.031M\", count(CASE\
  \ WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?)\
  \ THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"2.031M to 4.063M\", count(CASE WHEN\
  \ (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN\
  \ \"CR_RETURNING_CUSTOMER_SK\" END) AS \"4.063M to 6.094M\", count(CASE WHEN (\"\
  CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\"\
  \ END) AS \"6.094M to 8.125M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >=\
  \ ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END)\
  \ AS \"8.125M to 10.156M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND\
  \ \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"\
  10.156M to 12.188M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"\
  CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"12.188M\
  \ to 14.219M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\"\
  \ < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"14.219M to 16.250M\", count(CASE\
  \ WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?)\
  \ THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"16.250M to 18.281M\", count(CASE WHEN\
  \ (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN\
  \ \"CR_RETURNING_CUSTOMER_SK\" END) AS \"18.281M to 20.313M\", count(CASE WHEN (\"\
  CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\"\
  \ END) AS \"20.313M to 22.344M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\"\
  \ >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END)\
  \ AS \"22.344M to 24.375M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ?\
  \ AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS\
  \ \"24.375M to 26.406M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND\
  \ \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"\
  26.406M to 28.438M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"\
  CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"28.438M\
  \ to 30.469M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\"\
  \ < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"30.469M to 32.500M\", count(CASE\
  \ WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?)\
  \ THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"32.500M to 34.531M\", count(CASE WHEN\
  \ (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN\
  \ \"CR_RETURNING_CUSTOMER_SK\" END) AS \"34.531M to 36.563M\", count(CASE WHEN (\"\
  CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\"\
  \ END) AS \"36.563M to 38.594M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\"\
  \ >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END)\
  \ AS \"38.594M to 40.625M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ?\
  \ AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS\
  \ \"40.625M to 42.656M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND\
  \ \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"\
  42.656M to 44.688M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"\
  CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"44.688M\
  \ to 46.719M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\"\
  \ < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"46.719M to 48.750M\", count(CASE\
  \ WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?)\
  \ THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"48.750M to 50.781M\", count(CASE WHEN\
  \ (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN\
  \ \"CR_RETURNING_CUSTOMER_SK\" END) AS \"50.781M to 52.813M\", count(CASE WHEN (\"\
  CR_RETURNING_CUSTOMER_SK\" >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\"\
  \ END) AS \"52.813M to 54.844M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\"\
  \ >= ? AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END)\
  \ AS \"54.844M to 56.875M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ?\
  \ AND \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS\
  \ \"56.875M to 58.906M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND\
  \ \"CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"\
  58.906M to 60.938M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ? AND \"\
  CR_RETURNING_CUSTOMER_SK\" < ?) THEN \"CR_RETURNING_CUSTOMER_SK\" END) AS \"60.938M\
  \ to 62.969M\", count(CASE WHEN (\"CR_RETURNING_CUSTOMER_SK\" >= ?) THEN \"CR_RETURNING_CUSTOMER_SK\"\
  \ END) AS \"62.969M and up\" \nFROM \"7492547a05b84d643fd227e52ada40ff_sample\"\n\
  \ LIMIT ?"
checksum: 28019e2152b4bea16848a16389701796
queryDate: 1740614400000
usedBy:
  - TESTOPENMETADATA
tags: []
queryUsedIn:
  - id: e9119d1e-971c-4d36-8713-0798a54f4faa
    type: table
    name: CATALOG_RETURNS
    fullyQualifiedName: snowflake_test.SNOWFLAKE_SAMPLE_DATA.TPCDS_SF10TCL.CATALOG_RETURNS
    displayName: CATALOG_RETURNS
    deleted: false
processedLineage: false
service:
  id: 6d146814-0a2b-4296-ba8d-05f0c5888c40
  type: databaseService
  name: snowflake_test
  fullyQualifiedName: snowflake_test
